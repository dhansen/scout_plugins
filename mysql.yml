options:
  
  user:
    name: MySQL username
    default: root
    
  socket:
    name: MySQL socket
    default: /var/run/mysqld/mysqld.sock


  port:
    name: MySQL port

  host:
    name: MySQL host
    
  password:
    name: MySQL password
    default: frt
    
